enum FormFieldType {
  list,
  textInput,
  identificationInput,
  departmentCitySelector,
  imagePicker,
  datePicker,
  priceCalculator,
  mapSelector,
  checkboxListSelector
}

enum TextDynamicInputType {
  text,
  email,
  phone
}