enum FormFieldType {
  list,
  textInput,
  identificationInput,
  departmentCitySelector,
  imagePicker,
  datePicker,
  priceCalculator,
  mapSelector,
  checkboxListSelector,
  eventSelector,
  rolSelector,
  tableField
}

enum TextDynamicInputType {
  text,
  email,
  phone
}