enum FormFieldType {
  list,
  textInput,
  identificationInput,
  departmentCitySelector,
  imagePicker,
  datePicker,
  priceCalculator,
  mapSelector,
  checkboxListSelector,
  eventSelector,
  rolSelector
}

enum TextDynamicInputType {
  text,
  email,
  phone
}