enum FormFieldType {
  list,
  textInput,
  identificationInput,
  departmentCitySelector,
  imagePicker,
  datePicker,
  priceCalculator,
  mapSelector,
  checkboxListSelector,
  eventSelector
}

enum TextDynamicInputType {
  text,
  email,
  phone
}